spring:
  datasource:
    username: postgres
    password: 112358
    url: jdbc:postgresql://localhost/social
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    open-in-view: false

    hibernate:
      ddl-auto: none

    properties:
      hibernate:
        format_sql: true

    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql